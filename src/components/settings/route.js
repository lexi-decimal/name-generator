import Settings from '@/components/settings/Settings';

export default {
  meta: {
    title: 'Settings',
    sidebarBottom: true,
    icon: 'cogs',
    bgColor: 'var(--vuestro-indigo)',
  },
  name: 'settings',
  path: '/settings',
  component: Settings,
};